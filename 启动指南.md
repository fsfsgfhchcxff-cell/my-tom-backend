# 🚀 快速启动指南

## ✅ 已修复的问题

1. ✅ **端口冲突** - 已将端口从8080改为8081
2. ✅ **Null类型安全警告** - 已添加`@SuppressWarnings("null")`注解
3. ✅ **前端API地址** - 已更新为8081端口

## 🎮 启动应用

### 方法1: 使用Maven命令（推荐）
```bash
cd tom-learning-app
mvnw spring-boot:run
```

### 方法2: 使用IDE
1. 右键点击 `TomLearningAppApplication.java`
2. 选择 "Run" 或 "Debug"

## 🌐 访问地址

- 📱 **前端页面**: http://localhost:8081
- 💾 **H2数据库控制台**: http://localhost:8081/h2-console
- 🔧 **健康检查**: http://localhost:8081/api/game/ping

### H2数据库登录信息
```
JDBC URL: jdbc:h2:mem:tomdb
Username: sa
Password: (留空)
```

## 🧪 快速测试

打开浏览器或使用curl测试：

```bash
# 测试服务是否启动
curl http://localhost:8081/api/game/ping

# 获取用户数据（自动创建）
curl http://localhost:8081/api/game/home/1

# 查看所有商品
curl http://localhost:8081/api/items
```

## ⚠️ 如果还是无法启动

### 检查是否还有端口冲突
```bash
# Windows
netstat -ano | findstr :8081

# Linux/Mac
lsof -i :8081
```

### 如果8081也被占用，可以修改端口
编辑 `application.properties`，将端口改为其他值（如8082）：
```properties
server.port=8082
```

然后同步修改 `static/app.js` 中的API地址：
```javascript
const API_BASE = 'http://localhost:8082/api';
```

## 📱 开始使用

1. 打开浏览器访问 http://localhost:8081
2. 输入用户名注册/登录
3. 开始学习赚钻石！🎉

---

**预祝学习愉快！** 🐱💎

