# 🐱 汤姆猫学习版 - Tom Learning App

一个可爱的学习激励应用，通过学习和打卡赚取钻石，为你的汤姆猫购买食物、衣服和家具！

## 📸 项目展示

### 主要特性
- ✅ 学习计时系统（每10分钟获得1钻石）
- ✅ 每日签到奖励（10钻石）
- ✅ 商店系统（食物、衣服、家具）
- ✅ 背包管理和装备系统
- ✅ 可爱的三丽鸥风格界面

## 🚀 技术栈

### 后端
- **Spring Boot** 3.5.7
- **Spring Data JPA** - 数据持久化
- **H2 Database** - 内存数据库（可切换MySQL）
- **Lombok** - 简化代码
- **Maven** - 依赖管理

### 前端
- **React** 18.2.0
- **Tailwind CSS** 3.3.0 - 样式框架
- **Lucide React** - 图标库
- **Axios** - HTTP 客户端

## 📂 项目结构

```
tom-learning-app/
├── tom-learning-app/          # Spring Boot 后端
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/
│   │   │   │   └── com/mytom/tom_learning_app/
│   │   │   │       ├── entity/          # 实体类
│   │   │   │       ├── repository/      # 数据访问层
│   │   │   │       ├── service/         # 业务逻辑层
│   │   │   │       ├── controller/      # REST API
│   │   │   │       └── config/          # 配置类
│   │   │   └── resources/
│   │   │       ├── application.properties
│   │   │       └── static/              # 原生前端（备用）
│   │   └── test/
│   ├── pom.xml
│   ├── 快速启动.bat
│   └── Git推送后端.bat
│
└── frontend/                   # React 前端
    ├── public/
    ├── src/
    │   ├── components/
    │   │   ├── HomePage.js
    │   │   ├── StudyTimer.js
    │   │   ├── ShopPage.js
    │   │   ├── InventoryPage.js
    │   │   └── ApiTestPage.js
    │   ├── App.js
    │   ├── index.js
    │   └── index.css
    ├── package.json
    ├── tailwind.config.js
    └── Git推送前端.bat
```

## 🎮 快速开始

### 环境要求
- **Java 21+** （已测试 Java 23）
- **Node.js 16+**
- **Maven 3.6+**

### 1. 启动后端

```bash
# 方法1: 使用启动脚本（推荐）
双击运行：tom-learning-app/快速启动.bat

# 方法2: 使用命令行
cd tom-learning-app
mvnw.cmd spring-boot:run
```

**启动成功标志**：看到 `Started TomLearningAppApplication`

**后端地址**：http://localhost:8081

### 2. 启动前端

打开新的终端窗口：

```bash
# 进入前端目录
cd frontend

# 安装依赖（首次运行）
npm install

# 启动开发服务器
npm start
```

**前端地址**：http://localhost:3000

### 3. 访问应用

打开浏览器访问：**http://localhost:3000**

## 📡 API 接口

### GameController - 游戏主控制器

#### 获取用户首页数据
```http
GET /api/game/home/{userId}
```
自动创建用户并初始化100钻石

#### 增加用户钻石
```http
POST /api/game/diamonds/{userId}/add
Content-Type: application/json

{
  "amount": 50
}
```

### UserController - 用户管理

#### 创建用户
```http
POST /api/users
Content-Type: application/json

{
  "username": "小明"
}
```

#### 每日签到
```http
POST /api/users/{userId}/checkin
```

### StudyController - 学习系统

#### 开始学习
```http
POST /api/study/start
Content-Type: application/json

{
  "userId": 1
}
```

#### 结束学习
```http
POST /api/study/end/{sessionId}
```

### ShopController - 商店系统

#### 购买商品
```http
POST /api/shop/purchase
Content-Type: application/json

{
  "userId": 1,
  "itemId": 5
}
```

#### 装备物品
```http
POST /api/shop/equip
Content-Type: application/json

{
  "userId": 1,
  "inventoryId": 3
}
```

## 🗄️ 数据库设计

### User（用户表）
- `id` - 主键
- `username` - 用户名
- `diamond_balance` - 钻石余额
- `last_check_in` - 上次签到日期
- `total_study_minutes` - 总学习时长

### Item（商品表）
- `id` - 主键
- `type` - 类型（FOOD/CLOTH/FURNITURE）
- `name` - 名称
- `price` - 价格
- `image_url` - 图片URL
- `description` - 描述

### UserInventory（用户背包）
- `id` - 主键
- `user_id` - 用户ID
- `item_id` - 商品ID
- `is_equipped` - 是否装备
- `quantity` - 数量（食物）

## 🎨 设计风格

### 三丽鸥（Sanrio）风格特点
- 🎀 **马卡龙配色**：粉色、紫色、蓝色、黄色
- 🍬 **糖果按钮**：渐变色 + 3D边框 + 按下动画
- ✨ **流畅动画**：浮动、弹跳、脉冲效果
- 📐 **大圆角**：rounded-3xl (24px)
- 🌈 **柔和阴影**：shadow-lg + 渐变背景

## 🎯 游戏经济系统

- 💎 **新用户注册**：50钻石（UserController）/ 100钻石（GameController）
- 📅 **每日签到**：+10钻石
- 📚 **学习奖励**：每10分钟 +1钻石
- 🛍️ **商品价格**：2-120钻石

## 📦 商品列表

### 食物（2-8钻石）
🍕 披萨、🍔 汉堡、🍦 冰淇淋、🍰 蛋糕、🥤 可乐、🍎 苹果

### 衣服（10-50钻石）
👕 T恤、👔 西装、👗 连衣裙、🧥 外套、🎩 帽子、👟 运动鞋

### 家具（20-120钻石）
🛏️ 床、🪑 沙发、📺 电视、💡 灯、🖼️ 壁画、🌿 盆栽、📚 书架、🕰️ 时钟

## 🔧 配置说明

### 智能数据库配置

应用已配置为**智能切换数据库**：

- 🏠 **本地开发**：自动使用 H2 内存数据库
- ☁️ **云端部署**：自动切换到 PostgreSQL

**无需手动修改配置！**

#### 工作原理

1. **本地运行**（默认）：
   - 使用 `jdbc:h2:mem:testdb`
   - 端口：8081
   - H2 控制台：http://localhost:8081/h2-console

2. **云端部署**（Render）：
   - Render 自动注入 `DATABASE_URL` 环境变量
   - Spring Boot 自动识别并使用 PostgreSQL
   - 端口由 `PORT` 环境变量决定

#### 支持的数据库

- ✅ **H2** - 本地开发（已配置）
- ✅ **PostgreSQL** - 云端部署（已配置）
- ⚙️ **MySQL** - 需要手动配置

### 切换到 MySQL（可选）

如果需要使用 MySQL：

1. 修改 `application.properties`：

```properties
spring.datasource.url=jdbc:mysql://localhost:3306/tomdb
spring.datasource.username=root
spring.datasource.password=your_password
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
```

2. 添加 MySQL 依赖到 `pom.xml`：

```xml
<dependency>
    <groupId>com.mysql</groupId>
    <artifactId>mysql-connector-j</artifactId>
    <scope>runtime</scope>
</dependency>
```

## 🐛 常见问题

### Q: 端口被占用？
**A:** 运行 `快速启动.bat`，它会自动清理端口

### Q: 前端无法连接后端？
**A:** 
1. 确保后端在8081端口运行
2. 点击"测试"按钮检查连接
3. 查看 `package.json` 中的 `proxy` 配置

### Q: 数据丢失？
**A:** H2是内存数据库，重启会清空。切换到MySQL可持久化数据

## ☁️ 云端部署

### 快速部署到 Render + Vercel

**完整部署指南**：请查看 [云端部署指南.md](./云端部署指南.md)

#### 简要步骤

1. **推送代码到 GitHub**
   ```bash
   # 后端
   双击运行：Git推送后端.bat
   
   # 前端
   双击运行：frontend/Git推送前端.bat
   ```

2. **部署后端到 Render**
   - 创建 PostgreSQL 数据库
   - 连接 GitHub 仓库
   - 设置环境变量：`DATABASE_URL`
   - 自动部署！

3. **部署前端到 Vercel**
   - 连接 GitHub 仓库
   - 设置环境变量：`REACT_APP_API_URL`
   - 自动部署！

**费用**：完全免费！🎉

### 本地打包部署

#### 后端
```bash
# 打包
mvnw.cmd clean package

# 运行
java -jar target/tom-learning-app-0.0.1-SNAPSHOT.jar

# 使用生产配置
java -jar target/tom-learning-app-0.0.1-SNAPSHOT.jar --spring.profiles.active=prod
```

#### 前端
```bash
# 构建生产版本
npm run build

# build/ 目录可部署到任何静态服务器（如 Nginx、Apache）
```

## 📝 开发日志

### 已完成
- [x] 用户系统（注册、登录、签到）
- [x] 学习计时系统
- [x] 商店系统（购买、背包、装备）
- [x] 数据持久化（JPA + H2）
- [x] React前端（三丽鸥风格）
- [x] API测试页面
- [x] Git推送脚本

### 计划中
- [ ] 用户排行榜
- [ ] 学习提醒功能
- [ ] 好友系统
- [ ] 更多装饰物品
- [ ] 学习任务系统

## 📄 许可证

MIT License

## 👨‍💻 作者

Tom Learning App Team

---

**让学习变得超级可爱！** 🐱💖✨
